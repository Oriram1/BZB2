VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""
VITE_GOOGLE_MAPS_API_KEY=""

# Web Push. Generate with: npx web-push generate-vapid-keys
# Only the public key belongs here — the private key is a Supabase secret.
VITE_VAPID_PUBLIC_KEY=""

# End-to-end tests only. The authenticated screenshot walkthrough
# (e2e/auth-screenshot.spec.ts) skips itself unless both are set.
# Never commit real values — pass them on the command line or via CI secrets.
E2E_EMAIL=""
E2E_PASSWORD=""
