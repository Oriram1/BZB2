# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Real credentials never belong in a public repo. Use .env.example as the
# template — it lists every variable the app needs, with empty values.
.env
.env.*
!.env.example
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
supabase/.branches/

# Production backups contain private user data; never commit them.
backups/
